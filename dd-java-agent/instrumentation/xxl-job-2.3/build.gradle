plugins {
  id 'dd-trace-java.module.instrumentation'
}

muzzle {
  pass {
    group = "com.xuxueli"
    module = "xxl-job-core"
    versions = "[2.2.0,)"
    assertInverse = true
  }
}

addTestSuiteForDir('latestDepTest','test')

dependencies {
  compileOnly(group: 'com.xuxueli', name: 'xxl-job-core', version: '2.3.0')
}

tasks.withType(Test).configureEach {
  usesService(testcontainersLimit)
}
